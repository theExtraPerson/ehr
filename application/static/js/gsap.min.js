

// ADMIN DASHBOARD CHART.JS GSAP 

 // GSAP animations
 gsap.from(".card-metric", { duration: 1, y: 50, opacity: 0, stagger: 0.2, ease: "power3.out" });
 gsap.from("nav.sidebar a", { duration: 1, x: -100, opacity: 0, stagger: 0.1, ease: "power3.out" });
 gsap.from("main h2", { duration: 1, y: -20, opacity: 0, ease: "power3.out" });